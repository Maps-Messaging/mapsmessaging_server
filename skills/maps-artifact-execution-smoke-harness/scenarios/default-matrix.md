# Default Matrix Scenarios

1. `default-image-config`
- Start MAPS image with baked configuration.
- Verify startup and listeners.
- Verify MQTT publish/subscribe smoke.

2. `mounted-artifacts`
- Mount artifact directory manager YAML files into `/opt/maps/conf`.
- Verify startup and listeners with mounted config.
- Verify MQTT publish/subscribe smoke.
