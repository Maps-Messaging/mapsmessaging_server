steps:
  - label: ":maven: Build and release to github"
    command:
      - cd ./packaging/windows
      - powershell -Command "& { .\windows_packager.ps1 $(buildkite-agent secret get NEXUS_USER) $(buildkite-agent secret get NEXUS_PASSWORD) }"

    agents:
      queue: "windowsBuild"

