# MAPS Messaging Server Environment Configuration
# Copy this file to .env and modify as needed for your environment

# Server Configuration
MAPS_VERSION=latest
MAPS_HOME=/opt/maps
MAPS_DATA=/data

# JVM Settings
JAVA_OPTS=-Xmx2g -Xms1g -XX:+UseG1GC

# Network Ports (modify if needed)
MQTT_PORT=1883
HTTP_PORT=8080
MAIN_PORT=9000
NATS_PORT=4222
JMX_PORT=8778

# Security (for production use)
# CONSUL_URL= # Optional: for distributed configuration
# CONSUL_PATH= # Optional: configuration path in Consul
# CONSUL_TOKEN= # Optional: Consul access token