hawtio:
  -
    enable: true  # If true then the server will start the hawtio server else the server will not run the service
    authenticationEnabled: false # if true then hawtio service will require authentication
    warFileLocation: C:/Users/matthew/Downloads/hawtio-default-2.10.1.war # Path to the war file to load for the hawtio service
    host: 0.0.0.0  # Host name / ip address to bind to
    port: 8080  # Port for the hawtio service to bind to