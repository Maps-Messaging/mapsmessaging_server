CANBus integration tests require virtual CAN interfaces and are disabled by default.
Enable with: -Dmaps.tests.canbus=true

Satellite replication integration tests require the satellite daemon/configuration and are disabled by default.
Enable with: -Dmaps.tests.satellite=true
